<h1>Your Vocab Lists</h1>
<ul>
  <% @vocab_lists.each do |vl| %>
    <li><%= link_to vl.language.name, vocab_list_path(vl) %></li>
  <% end %>
</ul>

<%= link_to new_vocab_list_path do %>
  <i class="fas fa-plus-circle"> Add vocab</i>
<% end %>
