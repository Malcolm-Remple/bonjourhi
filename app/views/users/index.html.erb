<% @users.each do |user| %>
  <%= link_to user_path(user) do %>
    <%= render partial: 'shared/user-index-card', locals: {first_name: user.first_name, city: user.city, photo: user.photo} %>
   <% end %>
<% end %>
